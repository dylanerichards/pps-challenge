<%= form_for [@category, @idea] do |f| %>
  <% if @idea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@idea.errors.count, "error") %> prohibited this idea from being saved:</h2>

      <ul>
      <% @idea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <%# <div class="field"> %>
  <%#   <%= f.label :category_id %1><br> %>
  <%#   <%= f.text_field :category_id %1> %>
  <%# </div> %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
